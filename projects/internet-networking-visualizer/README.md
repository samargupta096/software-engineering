# 🌐 Internet & Networking Interactive Visualizer

An interactive web application to visualize how the internet works — from typing a URL to receiving a response. Built for software engineers who learn by seeing.

## 📋 Sections

| # | Section | What You'll Learn |
|---|---------|-------------------|
| 01 | **Network of Networks** | Internet topology, ISPs, IXPs, backbone networks |
| 02 | **DNS Resolution** | Step-by-step DNS lookup, record types (A, AAAA, CNAME, MX) |
| 03 | **TCP/IP & OSI Model** | Layer-by-layer encapsulation, OSI vs TCP/IP comparison |
| 04 | **TCP Handshake** | SYN/SYN-ACK/ACK sequence, connection teardown, TCP vs UDP |
| 05 | **HTTP Lifecycle** | Request/response with methods (GET/POST/PUT/DELETE), status codes |
| 06 | **TLS/SSL & HTTPS** | TLS 1.3 handshake, certificate chain, HTTP vs HTTPS comparison |
| 07 | **Routing & IP** | Traceroute simulation, TTL, IPv4 vs IPv6 |
| 08 | **Firewalls & NAT** | Packet filtering, NAT translation, forward vs reverse proxy |
| 09 | **CDNs & Caching** | Edge servers, cache HIT/MISS, latency comparison |
| 10 | **WebSockets** | HTTP polling vs WebSocket, upgrade handshake, overhead stats |

## 🚀 How to Run

```bash
# Option 1: Direct file
open index.html

# Option 2: Local server
npx -y serve .
```

## 🛠️ Tech Stack

- **HTML5** — Semantic structure
- **CSS3** — Custom properties, glassmorphism, animations
- **Vanilla JavaScript** — No frameworks, pure interactivity
- **Canvas API** — Network topology visualization
- **Google Fonts** — Inter + JetBrains Mono
